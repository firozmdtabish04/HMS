Run project: npm start

$env:NODE_OPTIONS = "--openssl-legacy-provider"; ng s -o
npm install -g @angular/cli
npm install
ng serve -o

$env:NODE_OPTIONS="--openssl-legacy-provider"
npx -p @angular/cli@16 ng serve -o